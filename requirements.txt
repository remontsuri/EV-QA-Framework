# EV-QA-Framework — Core Dependencies
# Python 3.8+

# Data processing & ML
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0
joblib>=1.3.0

# Validation & schemas
pydantic>=2.0.0

# Web dashboard
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0
jinja2>=3.1.0
aiofiles>=23.1.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# CAN bus protocol (optional: install only if using CAN emulation)
# python-can>=4.3.1

# TensorFlow for SOH prediction (optional: install only if using LSTM predictor)
# tensorflow>=2.15.0
